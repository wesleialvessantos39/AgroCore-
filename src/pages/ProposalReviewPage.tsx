import React from 'react';
import { ProposalDetailPage } from './ProposalDetailPage';

/**
 * Entrada dedicada da revisão. A página de detalhe continua concentrando a
 * apresentação do dossiê, enquanto a rota e o serviço impõem o escopo do
 * revisor atribuído.
 */
export const ProposalReviewPage: React.FC = () => <ProposalDetailPage />;
